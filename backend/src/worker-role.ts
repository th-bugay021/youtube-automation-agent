// Deprecated: the SERVICE_ROLE process split was removed — this deployment runs
// a single Render service (node dist/main.js) with one egress path, so there is
// no API/worker distinction to gate on. This file is an orphan pending deletion
// and is intentionally inert (no side effects).
export {};
